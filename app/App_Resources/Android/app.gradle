// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.libretto.app"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
